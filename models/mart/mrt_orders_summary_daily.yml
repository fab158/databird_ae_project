version: 2

models:
  - name: mrt_orders_summary_daily
    description: '{{ doc("mrt_orders_summary_daily") }}'
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - full_date
            - order_store_id
   